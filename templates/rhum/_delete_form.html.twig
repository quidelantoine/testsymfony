<form method="post" action="{{ path('rhum_delete', {'id': rhum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rhum.id) }}">
    <button class="btn">Delete</button>
</form>
